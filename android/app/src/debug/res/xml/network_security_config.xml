<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG builds only: allow plain HTTP to a backend on the developer's own
     machine (Laragon / "php artisan serve"), reached from the emulator as
     10.0.2.2 and from a USB device via "adb reverse" as localhost. Point the
     app there with the dart-define JUNTRA_API_BASE=http://10.0.2.2:8000/api.
     Release builds (both flavors) keep Android's default: HTTPS only. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
